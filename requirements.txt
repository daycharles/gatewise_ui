# Base requirements (non-GUI). For optional GUI support see requirements-gui.txt
# This default file avoids attempting to build heavy GUI wheels (like Kivy) during a standard pip install.

mfrc522>=0.0.7
spidev
RPi.GPIO
gpiozero
